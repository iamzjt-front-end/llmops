[pytest]
# 设置测试缓存文件存储在 tmp/.pytest_cache 中
cache_dir = tmp/.pytest_cache

# 设置默认的命令行选项
addopts = -v -s

# 内置工具提供者（google/duckduckgo/dalle/wiki）构建于 langchain-community 之上，
# 该包已被官方标记为日落且无独立替代包，此告警暂无法通过迁移消除，精准屏蔽
filterwarnings =
    ignore:`langchain-community` is being sunset.*:DeprecationWarning
